import { RestrictNumberInputDirective } from './restrict-number-input.directive';

describe('RestrictNumberInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictNumberInputDirective();
    expect(directive).toBeTruthy();
  });
});
